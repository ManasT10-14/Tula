# Tula — Legal Metrology compliance engine
# CPU-only inference by design. No CUDA anywhere in this file.

# --- core ---
pydantic>=2.6,<3
PyYAML>=6.0
python-dateutil>=2.9
tzdata>=2024.1                  # India assessment dates on Windows and minimal images

# --- web ---
fastapi>=0.110
uvicorn[standard]>=0.29
jinja2>=3.1
python-multipart>=0.0.9

# --- imaging / metrology ---
numpy>=1.26,<3
opencv-contrib-python>=4.9        # contrib build: needed for cv2.aruco (scale card)
Pillow>=10.2

# --- reporting ---
reportlab>=4.1                    # PDF (pure python, installs clean on Windows)
python-docx>=1.1                  # editable DOCX export
pypdf>=5.0,<7                    # machine-readable PDF attachment
pillow-heif>=0.18                # HEIC uploads from phones
zxing-cpp>=2.2                   # image barcode decoding (CPU)

# --- OCR (CPU / ONNX Runtime) ---
# Real OCR is the default. Missing models produce explicit warnings and
# inconclusive results. Fixture sidecars require explicit engine selection.
rapidocr-onnxruntime>=1.3.24
